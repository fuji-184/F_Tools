[package]
name = "main"
version = "0.1.0"
edition = "2024"

[dependencies]
ftool = { path = "../", features = ["async_cross_thread", "libc"] }
tokio = { version = "1.52.2", features = ["full"] }
tokio-scoped = "0.2.0"

[dev-dependencies]
#ftest = { git = "https://github.com/fuji-184/FTest" }

